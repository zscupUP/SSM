<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zsc.ssm.mapper.EmployeeMapper">
    <!--int insertUser();-->
    <insert id="insertUser">
        
    </insert>

    <!--User selectUser-->
    <select id="selectUser" resultType="com.zsc.mybatis.pojo.User">
        select * from  t_user where id = 1;
    </select>
</mapper>